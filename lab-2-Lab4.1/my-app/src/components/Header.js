//made the start of the code calling on the constant 
const Header = () => {
    //made it so the text would be center and a bit of text 
    return <h1 className="text-center">My Header in another component</h1>;
  };
  // this is where i export the output of the header
  export default Header;